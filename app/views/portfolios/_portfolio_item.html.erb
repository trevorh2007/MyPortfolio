<div class="card">
  <%= image_tag portfolio_item.thumb_image unless portfolio_item.thumb_image.nil? %>
  <p class="card-text">
  	<span><%= link_to portfolio_item.title, portfolio_show_path(portfolio_item) %></span>
  	<span><%= portfolio_item.subtitle %></span>

  	 <%= link_to "New Portfolio Item", new_portfolio_url if logged_in?(:site_admin) %>
      <%= portfolio_item.body %>
      <%= link_to "Edit", edit_portfolio_path(portfolio_item) if logged_in?(:site_admin) %>
      <%= link_to 'Delete', portfolio_path(portfolio_item), method: :delete, data: { confirm: 'Are you sure?' } if logged_in?(:site_admin) %>
  </p>
</div>